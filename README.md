# Pothole Detection with YOLO & FastAPI Deployment
Building a robust computer vision pipeline to automatically detect potholes in road images using fine-tuned YOLO models. This end-to-end project includes data annotation, training multiple object detection models, and serving them via a FastAPI backend.
